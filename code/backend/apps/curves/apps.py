from django.apps import AppConfig


class CurvesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.curves"
    label = "curves"
    verbose_name = "Yield Curves"
